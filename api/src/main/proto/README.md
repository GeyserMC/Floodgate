ProtoBuf definitions are taken from Connect main
repo https://github.com/minekube/connect/tree/main/api. We keep them in sync manually until we have
a more clean way, though changes in our proto are rare.